= form_for @status do |f|
  -if @status.errors.any?
    #error_explanation
      %h2= "#{pluralize(@status.errors.count, "error")} prohibited this status from being saved:"
      %ul
        - @status.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :statusname
    = f.text_field :statusname
  .actions
    = f.submit 'Save'
