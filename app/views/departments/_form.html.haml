= form_for @department do |f|
  -if @department.errors.any?
    #error_explanation
      %h2= "#{pluralize(@department.errors.count, "error")} prohibited this department from being saved:"
      %ul
        - @department.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :depname
    = f.text_field :depname
  .actions
    = f.submit 'Save'
